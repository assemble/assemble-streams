'use strict';

/**
 * Mdule dependencies
 */

var utils = require('lazy-cache')(require);
var fn = require;

require = utils;
require('through2', 'through');
require('src-stream');
require = fn;

/**
 * Expose `utils
 */

module.exports = utils;
